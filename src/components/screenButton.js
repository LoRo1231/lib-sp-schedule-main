import React from 'react';

function ScreenButton (){

    return(
        <div>
            <button type="button" class="btn btn-secondary"> Screen(s) </button>
        </div>
    );
}

export default ScreenButton;